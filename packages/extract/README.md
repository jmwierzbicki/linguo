# extract

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build extract` to build the library.

## Running unit tests

Run `nx test extract` to execute the unit tests via [Jest](https://jestjs.io).
