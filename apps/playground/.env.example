# Copy to .env and fill in your key. .env is gitignored — never commit a key.
# Google Gemini API key, used by linguo.translator.mjs for `linguo-extract translate`.
# Get one at https://aistudio.google.com/apikey
GEMINI_API_KEY=
